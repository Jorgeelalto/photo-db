## photo-db

### Get started

In order to start the program you need a `config.yml` file in the root of the repository, next to `main.py`. This YAML file should contain at least these variables:

```yaml
photo_folder: "/Users/jsmith/Photos"
```
